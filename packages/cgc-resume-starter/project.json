{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "cgc-resume-starter",
  "//": "Typst resume starter template, published to Typst Universe via the publish workflow",
  "root": "packages/cgc-resume-starter",
  "sourceRoot": "packages/cgc-resume-starter",
  "projectType": "library",
  "targets": {
    "lint": {
      "//": "Validate package structure and manifest against Typst Universe submission requirements",
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "VERSION=$(node -p \"require('./.github/tool-versions.json')['typst-package-check']\") && docker run --rm -v \"$PWD/packages/cgc-resume-starter:/data\" \"ghcr.io/typst/package-check:$VERSION\" check"
        ],
        "parallel": false
      } },
    "test": {
      "//": "Placeholder — replace with tytanic or other test runner when test suite is added",
      "executor": "nx:run-commands",
      "options": {
        "commands": [ "echo 'No tests configured yet for cgc-resume-starter'" ],
        "parallel": false
      } }
  },
  "tags": [ ]
}
